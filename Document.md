# WheatMarket 需求文档

## 1. 项目概述

**WheatMarket** 是一款 Minecraft 市场 MOD，玩家可在游戏内通过“笔记本电脑”方块访问虚拟市场，进行物品买卖、货币交易等操作。

当前项目为 NeoForge 单平台单模块项目，目标 MC 1.21.1，Java 21。主要依赖 LDLib2、H2 嵌入式数据库。经济与市场数据通过 H2
持久化，专用服务器路径位于 `./config/WheatMarket/database/WheatMarketDB`，单人/客户端路径位于对应存档目录下。

## 2. 当前实现状态

| 模块      | 功能                                                                                                                    | 状态                                                      |
|---------|-----------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------|
| 方块      | 笔记本电脑方块：方向放置、开合、电源切换、水淹支持、右键打开市场菜单                                                                                    | 已完成                                                     |
| 方块/物品模型 | 笔记本电脑开/关/物品形态；交易板模型和贴图                                                                                                | 笔记本电脑已完成；交易板仅有资源，未注册                                    |
| 音效      | 笔记本电脑开/关、翻页、铅笔书写、订单盖章                                                                                                 | 已完成                                                     |
| 命令      | `/balance` 查询余额                                                                                                       | 已完成                                                     |
| 命令      | `/pay <player> <amount>` 转账                                                                                           | 已完成                                                     |
| 命令      | `/account <player> add\|set\|remove <amount>` 管理员操作                                                                   | 已完成                                                     |
| 测试命令    | `/wmtest data`、`/wmtest listings`、`/wmtest enchanted_wheat`、`/wmtest limited_wheat`、`/wmtest limited_diamond_pickaxe` | 已完成，主要用于开发自检和造测试数据                                      |
| 数据库     | H2 连接管理、服务端/客户端路径区分、表创建、关闭流程                                                                                          | 已完成                                                     |
| 数据库     | `player_info` 表 CRUD、repository、事务化 service                                                                           | 已完成，初始余额仍固定为 0                                          |
| 数据库     | `market_item` 基础 CRUD、缓存加载、service 层过滤/搜索/排序/分页、购买/上架/管理 mutation                                                     | 部分完成；有限库存售罄会删除挂单；repository 层仍无条件查询/分页 SQL；过期清理和库存退回未实现 |
| 数据库     | `purchase_record` insert、最近购买时间、时间窗口内购买数量、时间窗口内最早购买时间                                                                 | 已可支持当前冷却/限购判断；交易历史列表查询未实现                               |
| 数据库     | 收购单成交后的待领取物品托管仓库                                                                                                      | 未完成；应以数据库为权威存储，不直接写入离线玩家背包                              |
| 缓存      | `MarketItemCache` 启动时从数据库加载，事务成功后由 service 更新                                                                         | 已完成当前架构所需能力                                             |
| 网络      | NeoForge Custom Payload 注册和发送工具                                                                                       | 已完成                                                     |
| 网络      | C2S：市场列表请求、购买、上架、商品管理、物品选择状态配置同步                                                                                      | 已完成服务端处理；上架/管理尚无完整 GUI 入口                               |
| 网络      | S2C：市场列表、操作结果、余额更新                                                                                                    | 已完成；独立商品详情包未实现                                          |
| GUI     | LDLib2 市场主界面：标题栏、筛选、搜索、排序方向、只看自己商品、余额、分页、商品卡片                                                                         | 部分完成；卡片字段较少；上架按钮已进入物品选择子界面，但完整上架表单未接入                   |
| GUI     | 商品卡片渲染真实 `MarketItem` 列表                                                                                              | 部分完成；当前显示交易类型、图标、价格，未直接显示库存、卖家、上架时间等完整信息                |
| GUI     | 出售单购买确认界面：商品摘要、数量选择、余额显示、限制提示、确认动画、购买请求                                                                               | 部分完成；仅支持出售单，收购单会提示暂不支持                                  |
| GUI     | 通用 LDLib2 物品选择下级界面：复用 `WheatMarketMenu` 选择槽和玩家背包槽、父级指定用途/模式/初始物品/锁定模板、确认或返回父级、父级菜单暗色背景                                | 已完成字段编辑器基础能力；尚未接入完整上架/管理表单                              |
| GUI     | 上架界面                                                                                                                  | 未完成；后端 packet/service 已有基础能力                            |
| GUI     | 商品详情/管理界面                                                                                                             | 未完成；后端 packet/service 已有补货、取出、改价、下架、OP 设置能力             |
| 异步      | 单线程 DB executor，写操作通过 `CompletableFuture` 串行执行                                                                        | 已完成；市场列表请求仍会在 service 中同步读取余额/冷却信息                      |
| 国际化     | 中英文语言文件，覆盖当前命令、主界面、确认界面和操作结果                                                                                          | 已完成当前功能；未来新界面仍需补键                                       |

## 3. 待实现功能需求

### 3.1 界面需求

#### 3.1.1 主页面完善

**当前状态**：LDLib2 市场主界面已显示真实市场列表，并已接入市场列表网络请求。筛选、搜索、排序、分页、只看自己商品和余额显示均已有实现。

**剩余需求**：

- 商品卡片补充展示库存数量、卖家/系统商店标识、上架时间、最后交易时间、冷却/限购状态等关键信息
- 系统商店商品增加更明确的视觉标识，例如边框、角标或图标
- 上架按钮已打开通用物品选择子界面并保留临时物品草稿；仍需接入价格、数量、交易类型、OP 选项并发送 `ListItemC2SPacket`
- 商品卡片点击后的行为区分出售单和收购单：出售单进入购买确认，收购单进入供货/成交流程
- 主界面接收到成功操作结果后会刷新当前市场列表；未来详情/管理界面仍需做局部刷新或返回后刷新
- 卖家名称解析需要更完整，避免只能显示 UUID 片段

#### 3.1.2 出售单购买确认界面

**当前状态**：已实现出售单购买确认界面。界面显示商品、卖家/系统、单价、名称、上架时间、库存、余额、冷却/限购提示；支持输入数量、加减按钮、确认购买、成功签名/盖章动画；确认后发送
`BuyItemC2SPacket`。

**剩余需求**：

- 根据余额在客户端提前禁用或提示不可购买，当前主要依赖服务端失败结果
- 背包满时目前会把物品掉落到地上，仍需按需求改为明确提示或可配置处理
- 冷却失败提示可进一步显示剩余可购买数量和剩余时间
- 支持从确认界面返回时保留主页面筛选/分页状态

#### 3.1.3 上架界面

**当前状态**：`ListItemC2SPacket` 和 `MarketService.listItem(...)` 已存在，服务端可创建市场商品记录，并在玩家商品上架成功后扣减背包物品。主页面“上架”按钮已进入通用
LDLib2 物品选择子界面。该界面现在作为父级表单的物品字段编辑器，通过 `ItemSelectionRequest`
指定用途、固定模式、初始选择、数量基线、锁定模板和确认回调；通过
`ItemSelectionResult` 返回物品模板、总数量和相对基线变化量。`selectedStack` 只作为模板保存，数量必须使用 `totalAmount`
，避免把超过原版序列化上限的聚合数量写进 `ItemStack.count`。当前主页面临时以 `LIST_SELL + TRANSFER`
打开该界面并保留物品草稿；价格、数量、交易类型和 OP 选项表单尚未实现，物品选择结果也尚未接入最终 `ListItemC2SPacket` 提交流程。

**需求**：

- 从主页面“上架”按钮进入完整上架流程，保留现有通用物品选择子界面作为其中一步
- 物品选择界面是上架流程的下级页面，不是独立测试入口；父级上架表单决定 `purpose`、`mode`、`initialSelection`、
  `baselineAmount`、`lockedStackTemplate`、`allowEmpty` 和如何消费选择结果
- 物品选择界面不提供“收取/取样”切换按钮，只显示父级指定的固定模式，并提供“确认”和“返回”
- “确认”把当前选择槽状态作为 `ItemSelectionResult` 回传父级表单，但不直接发送上架 packet
- 父级表单保存选择结果时必须将 `selectedStack` 当作 count=1 的物品模板，将数量单独保存在 `totalAmount`
- “返回”放弃本次子页面修改，回到父级表单
- 如果父级表单已有草稿物品，重新进入物品选择界面时应通过 `initialSelection` 恢复选择槽
- 出售订单上架时，进入物品选择下级页面应自动使用“收取”模式（`ItemSelectionMode.TRANSFER`
  ），玩家把实际要出售的物品转入选择槽，确认后父级表单记录待上架物品和数量
- 收购订单上架时，进入物品选择下级页面应自动使用“取样”模式（`ItemSelectionMode.SAMPLE`
  ），选择槽只记录精确样品模板，不扣除玩家样品物品，确认后父级表单记录收购目标模板
- 显示玩家物品栏，玩家可选择要上架的物品或收购样品；当前选择槽 UI 已存在，仍需把选择结果绑定到完整上架表单
- 支持输入数量和价格
- 支持选择交易类型：出售或收购
- 出售模式：玩家提交物品，成交时买家支付货币并获得物品
- 收购模式：使用精确样品创建收购单，界面显示玩家物品栏和样品框，玩家从物品栏拖动一个样品到样品框
- 收购样品框只复制物品模板，不扣除样品物品；样品的 `itemID` 和 `itemNBTCompound` 用于定义收购目标
- 收购单不提供普通物品 ID 选择器；如果玩家没有样品物品，则不能创建该精确收购单
- 收购模式需要重新定义资金预留、供货物品校验、成交后货币流向，不能沿用出售模式的扣物品逻辑
- OP 专属选项：系统商店、无限数量、冷却数量、冷却时间
- 成功后发送 `ListItemC2SPacket` 并刷新市场列表
- 上架前后都必须校验物品槽位、数量、NBT/components 和物品一致性，防止并发换物品导致错误扣减
- 收购单创建请求不能信任客户端传来的任意 `itemNBTCompound`；服务端应根据样品来源槽位重新读取玩家背包中的实际 `ItemStack`
  并序列化

#### 3.1.4 商品详情及商品管理界面

**当前状态**：`ManageItemC2SPacket` 和 service 层已支持补货、取出、改价、下架、切换系统商店、切换无限数量、设置冷却；但没有对应
GUI。

**需求**：

- 点击商品的“详情/管理”入口后进入该界面
- 显示商品详细信息：图标、名称、描述、价格、数量、卖家、上架时间、最后交易时间、冷却/限购规则
- 补货/编辑库存流程复用物品选择下级页面，且只能使用“收取”模式（`ItemSelectionMode.TRANSFER`）
- 补货/编辑库存本质是对已上架出售商品的库存字段编辑，不只是追加新增补货数量
- 补货目标为出售订单时，进入物品选择下级页面应把当前剩余库存对应的 `ItemStack` 作为 `initialSelection` 预填到选择槽
- 补货/编辑库存时必须把原商品模板作为 `lockedStackTemplate` 传入，选择槽非空时只能放入与原商品
  `ItemStack.isSameItemSameComponents(...)` 一致的物品
- 补货/编辑库存允许清空选择槽，表示父级管理表单将目标库存编辑为 0 或准备取回全部剩余库存；具体业务提交由父级管理表单决定
- 补货预填的剩余库存不等同于玩家本次新增补货数量；父级管理表单应使用 `baselineAmount`、`totalAmount` 和 `deltaAmount`
  区分已有库存、编辑后库存和新增/减少数量
- 显示玩家物品栏，用于补货和取出操作
- 卖家本人或 OP 可执行补货、取出、改价、下架
- OP 可执行系统商店切换、无限数量切换、冷却设置
- 补货必须在客户端菜单和服务端最终提交时同时校验补入物品与原商品一致
- 取出和下架时需要处理背包满的情况
- 操作成功后刷新详情界面和市场列表

### 3.2 数据与网络需求

#### 3.2.1 网络通信系统

**当前状态**：已迁移到 NeoForge Custom Payload API。当前注册的包包括：

- C2S：`RequestMarketListC2SPacket`、`BuyItemC2SPacket`、`ListItemC2SPacket`、`ManageItemC2SPacket`、
  `SetItemSelectionModeC2SPacket`
- S2C：`MarketListS2CPacket`、`OperationResultS2CPacket`、`BalanceUpdateS2CPacket`

**剩余需求**：

- 上架界面接入 `ListItemC2SPacket`
- 商品详情/管理界面接入 `ManageItemC2SPacket`
- 物品选择下级界面由具体父流程读取 `ItemSelectionResult`，并在上架/管理表单中转换为对应 packet 参数
- `SetItemSelectionModeC2SPacket` 同步物品选择菜单的固定模式、初始选择和锁定模板；最终上架/补货提交仍必须由对应业务 packet
  和服务端校验完成
- 如商品详情需要服务端实时数据，应新增商品详情 request/response 包，或在市场列表返回中补足详情字段
- 操作成功后主动刷新市场列表、详情界面和余额，避免只依赖玩家手动刷新
- 收购单成交需要新增或扩展网络包，避免与出售购买流程混用
- 收购单创建需要让客户端传递样品来源槽位或通过真实菜单槽位同步，服务端必须以玩家背包中的实际样品为准生成
  `itemNBTCompound`

#### 3.2.2 数据库 CRUD 和查询能力

**当前状态**：`market_item` repository 只有基础 CRUD；市场列表的条件过滤、搜索、排序和分页当前在 `MarketService` 基于
`MarketItemCache` 完成。`purchase_record` 已支持当前冷却系统需要的时间窗口查询。

**剩余需求**：

- 明确是否继续以缓存为主做市场查询；如果市场规模扩大，需要补充 repository 层条件查询和分页 SQL
- `MarketItemRepository` 可补充按条件查询、分页查询、可用商品查询、过期商品查询
- `PurchaseRecordRepository` 补充按买家/商品查询交易记录列表，用于历史记录和管理界面
- 保持缓存只在 service 事务成功后更新，不恢复“缓存全量落库”作为主要持久化机制
- 库存为 0 的商品应自动下架或至少从市场列表隐藏

#### 3.2.3 收购单待领取物品托管

**当前状态**：未实现。收购单发布者离线时，其他玩家向该收购单出售物品后，不能直接把物品写入离线玩家背包。

**设计要求**：

- 收购单成交所得物品进入市场托管仓库，由收购单发布者上线后在自建领取界面中领取
- 托管仓库以 H2 数据库为权威存储；界面只作为托管记录的视图，不使用玩家 `Inventory`、临时 `SimpleContainer` 或方块背包作为持久化来源
- 领取时由 service 执行校验和事务：确认托管记录存在、玩家背包可容纳、发放物品、删除或减少托管记录
- 背包满时不删除托管记录；可以拒绝领取，或只领取可放入数量并保留剩余数量
- 收购单取消或过期时，只退还未成交部分的冻结资金；已成交但未领取的物品继续保留在托管仓库

**建议表结构**：

- `deliveryID VARCHAR(36) PRIMARY KEY`
- `receiverID VARCHAR(36) NOT NULL`：待领取玩家，即收购单发布者
- `marketItemID VARCHAR(36)`：来源收购单，可为空以兼容未来迁移或被删除的订单
- `sourcePlayerID VARCHAR(36)`：供货玩家
- `itemID VARCHAR(255) NOT NULL`：单独保存物品 ID，便于查询、展示、统计和迁移
- `itemNBTCompound CLOB`：沿用现有项目字段名，保存 `ItemStack.save(...)` 得到的完整物品栈数据
- `amount INT NOT NULL`：单独保存数量，便于部分领取和避免只为数量解析 NBT
- `createdTime TIMESTAMP DEFAULT CURRENT_TIMESTAMP`
- `claimedTime TIMESTAMP` 或 `remainingAmount INT`：根据实现选择“整条领取删除”或“允许部分领取”

#### 3.2.4 数据库操作异步化与队列

**当前状态**：经济和市场写操作已通过 `CompletableFuture` + 单线程 DB executor 串行执行；服务器关闭时会等待 executor
结束并关闭数据库连接。

**剩余需求**：

- 避免高频市场列表请求在主线程执行过重同步查询
- 服务器关闭前需要更明确地保证未完成 DB 操作完成或可追踪失败
- 为关键异步操作补充日志和失败回调，便于排查数据不一致

#### 3.2.5 数据库存档保存

**当前状态**：经济和市场数据在业务发生时通过 service/repository 事务提交；世界保存不是主要持久化机制。

**剩余需求**：

- 可在世界保存或定时任务中执行过期商品清理、统计数据写入、缓存/数据库一致性检查
- 服务器关闭前应尽量完成所有未完成数据库操作

### 3.3 整合包支持需求

#### 3.3.1 Config 配置商品列表

**当前状态**：未实现。

**需求**：

- 支持从 config 文件读取预定义商品列表
- 格式建议使用 JSON 或 TOML，每个条目包含物品 ID、NBT/组件数据、价格、数量、是否服务器商店、是否无限数量、冷却设置
- 服务器启动时自动加载配置，将配置中的商品注册到市场，避免重复创建
- 支持通过命令热重载配置
- 明确配置商品与玩家/系统商店、无限库存、过期规则的关系

### 3.4 经济系统需求

#### 3.4.1 货币系统

**当前状态**：`player_info` 表存储余额，`/balance`、`/pay`、`/account` 已实现。出售单购买时会扣减买家余额；非系统商店会增加卖家余额，系统商店当前相当于销毁买家支付的货币。

**剩余需求**：

- 初始余额可配置，当前固定为 0
- 系统商店货币流向可配置：销毁、注入指定账户、进入公共账户等
- 收购模式需要定义资金预留和成交后的余额变化
- 继续保持 service 层拒绝 `NaN`、`Infinity`、负数和其他非法金额

#### 3.4.2 冷却和限购系统

**当前状态**：出售单购买时已根据 `cooldownAmount` 和 `cooldownTimeInMinutes` 查询 `purchase_record`
，并阻止超出限制的购买。市场列表也会返回当前玩家在时间窗口内的剩余可购买数量。

**剩余需求**：

- 改善客户端展示，明确显示限购数量、周期、剩余额度和失败原因
- 支持收购单成交的冷却/限购判断
- 补充更完整的测试覆盖，尤其是时间窗口边界和多次购买累计数量

#### 3.4.3 商品过期

**当前状态**：`MarketItem.isExpired()` 已实现；市场列表会过滤过期商品，购买时也会拒绝过期商品。

**剩余需求**：

- 过期商品自动下架
- 玩家商品过期后库存退回卖家，或设计离线退回/邮件/临时仓库机制
- 系统商店过期后的处理规则需要单独定义

### 3.5 收购模式需求

**当前状态**：数据模型中 `ifSell` 字段可区分出售/收购；主界面可以筛选和显示收购标签；购买确认界面遇到收购单会提示暂不支持成交。当前上架
packet 虽可传 `ifSell=false`，但收购单的经济、物品流向、资金冻结和待领取托管尚未完整实现。

**需求**：

- 出售模式：卖家挂出物品，买家支付货币获得物品
- 收购模式：买家挂出收购单，供货玩家提供物品并获得货币
- 收购单上架时必须从玩家物品栏样品框读取精确样品，创建目标物品模板
- 收购单样品不扣除，只用于保存 `itemID` 和 `itemNBTCompound`；`amount` 表示剩余收购需求量
- 收购单只支持精确匹配，供货时必须校验提交物品与收购单样品模板一致
- 收购单上架时需要冻结或校验发布者资金
- 成交后从预留资金或发布者账户向供货玩家付款
- 成交后物品写入待领取托管仓库，归属给收购单发布者，不能直接写入离线玩家背包
- 收购单发布者通过自建领取界面领取托管物品；领取失败时托管记录必须保留
- 收购单的库存/需求量、冷却、过期、退款规则需要与出售单分开定义

### 3.6 其他需求

#### 3.6.1 交易板方块

**当前状态**：模型和贴图已存在，但无 Java 类和注册。

**需求**：

- 实现 `TradingBoardBlock`，可作为墙壁挂载方块
- 右键打开市场界面，或设计为只读的市场公告/热门商品界面
- 补充方块状态、物品注册、loot table、语言键和模型绑定

#### 3.6.2 国际化补全

**当前状态**：当前已实现功能的主要中英文语言键已存在，包括 `gui.wheatmarket.searchbar`、购买确认、操作结果和测试命令。

**需求**：

- 为上架界面、详情/管理界面、收购成交流程、配置热重载命令补充中英文翻译
- 新增语言键时同步维护 `zh_cn.json` 和 `en_us.json`

---

## 4. 优先级排序

| 优先级 | 功能                                | 原因                           |
|-----|-----------------------------------|------------------------------|
| P0  | 上架流程补全并接入 `ListItemC2SPacket`     | 物品选择子界面已有，但玩家仍无法填写价格/数量并创建商品 |
| P0  | 商品详情/管理界面接入 `ManageItemC2SPacket` | 当前已有后端能力，但卖家无法从 GUI 管理商品     |
| P0  | 出售购买流程收尾：客户端余额预校验、失败提示细化、返回状态保留   | 已可购买，剩余主要是体验和提示问题            |
| P1  | 商品卡片信息完善                          | 主页面可用性和判断成本依赖卡片信息            |
| P1  | 收购模式完整设计与实现                       | 数据模型已有入口，但成交流程未完成            |
| P1  | 背包满、并发换物品、补货物品一致性等交易安全边界          | 防止交易结果与玩家预期不一致               |
| P1  | 收购单待领取物品托管仓库和领取界面                 | 离线收购成交必须有可靠的物品归属和领取机制        |
| P2  | 过期商品自动下架和库存退回                     | 数据清理和玩家资产安全                  |
| P2  | Config 预定义商品列表和热重载                | 整合包和服务器配置需求                  |
| P2  | 交易历史查询和管理展示                       | 后续详情页、审计和玩家记录需要              |
| P3  | 交易板方块                             | 辅助入口，不阻塞核心市场流程               |
| P3  | repository 层条件查询/分页 SQL           | 当前缓存查询可用，规模扩大后再优先处理          |
| P3  | 未来界面和命令的语言键补全                     | 随功能增加持续维护                    |

## 5. 已知问题

1. 上架完整表单和商品详情/管理界面仍未完成；上架按钮目前只进入 `LIST_SELL` 物品选择子界面并保留物品草稿，尚未填写价格/数量并发送上架
   packet。
2. 出售单购买确认界面已实现，但收购单成交仍未实现。
3. 收购单待领取物品托管仓库和领取界面仍未实现，离线收购成交规则尚未落地。
4. 商品卡片目前信息较少，未直接展示库存、卖家、上架时间、系统商店标识等完整字段。
5. 有限库存商品售罄后已自动删除挂单；过期商品自动下架和库存退回仍未实现。
6. “无限数量”已改为独立 `ifInfinite` 字段；旧 `Integer.MAX_VALUE` 数据仅作为启动迁移兼容来源。
7. 补货流程需要用 `lockedStackTemplate` 锁定原商品物品种类和 NBT/components，并在最终业务提交时再次校验，避免补错物品。
8. 背包满时购买/取出当前可能掉落物品，尚未按需求提供明确提示或可配置处理。
9. 货币继续使用 `double` / SQL `DOUBLE`，必须保持 service 层拒绝 `NaN`、`Infinity` 和非法金额。
10. 物品数据仍以 `ItemStack.save(...)` 得到的 NBT 写入数据库，缺少统一稳定的 `ItemStack` codec/迁移策略。
11. `market_item` repository 仍未提供完整条件查询/分页查询，当前依赖 service/cache 层过滤。
12. Config 商品列表、过期商品退回、服务器商店货币流向配置仍未实现。
